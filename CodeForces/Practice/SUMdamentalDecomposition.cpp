#include <bits/stdc++.h>
using namespace std;

int main() {
    int t, n, x;
    cin >> t;

    // when there are an odd number of 1's the output is 1
    // when there are an even number of 1's the output is 0
    for(int i = 0; i < t; i++){
        cin >> n >> x;

                
    }

    return 0;
}